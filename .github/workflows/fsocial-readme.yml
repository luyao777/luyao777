name: Social Readme

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  update-social:
    runs-on: ubuntu-latest
    steps:
      - uses: luyao777/social-readme@master
        with:
          blog_rss_link: https://feed.cnblogs.com/blog/u/249615/rss/
